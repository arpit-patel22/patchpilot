# Dev-only overrides (active when SPRING_PROFILES_ACTIVE=dev or no profile set on dev machines)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update
